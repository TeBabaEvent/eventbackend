# ============================================
# Nixpacks Configuration for Railway
# WeasyPrint System Dependencies
# ============================================

[phases.setup]
nixPkgs = [
    "python311Full",
    # WeasyPrint dependencies
    "pango",
    "cairo", 
    "gdk-pixbuf",
    "glib",
    "libffi",
    "fontconfig",
    "freetype",
    "harfbuzz",
    "fribidi",
    # Fonts for PDF rendering
    "liberation_ttf",
    "dejavu_fonts"
]

[phases.install]
cmds = [
    "python -m venv /opt/venv",
    "/opt/venv/bin/pip install --upgrade pip",
    "/opt/venv/bin/pip install -r requirements.txt"
]

[start]
cmd = "LD_LIBRARY_PATH=$HOME/.nix-profile/lib:$LD_LIBRARY_PATH /opt/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2"
